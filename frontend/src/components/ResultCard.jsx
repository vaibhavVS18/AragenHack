/**
 * ResultCard — one lab result, fully explained.
 *
 * The main Explainable-AI surface: badge, measured value, reference
 * range with a visual position marker, deviation, rule fired, the LLM's
 * clinical explanation, and the suggested next step.
 *
 * TODO(step 5): card layout + range visualization.
 */
export default function ResultCard() {
  return null;
}
