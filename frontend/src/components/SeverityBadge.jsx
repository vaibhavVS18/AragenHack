/**
 * SeverityBadge — the color-coded status pill.
 *
 *   critical -> red     🚨
 *   warning  -> yellow  ⚠️
 *   normal   -> green   ✓
 *
 * Color is never the only signal: each badge also carries an icon and a
 * text label so the UI stays readable for colorblind users.
 *
 * TODO(step 5): SEVERITY_STYLES map + badge markup.
 */
export default function SeverityBadge() {
  return null;
}
