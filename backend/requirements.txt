# ---- Web layer ----
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12       # required by FastAPI for CSV file uploads
pydantic>=2.9.0
pydantic-settings>=2.6.0

# ---- MCP (Model Context Protocol) ----
mcp>=2.1.0                     # official Python SDK: server + client (v2 API: MCPServer)

# ---- LLM provider ----
google-genai>=0.8.0            # Gemini SDK (explanation layer)

# ---- Config / utils ----
python-dotenv>=1.0.1

# ---- Dev / test ----
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0                  # also used by FastAPI TestClient
reportlab>=4.2                  # server-side PDF report generation
pypdf>=5.0                     # dev: asserts the generated PDF is valid
numpy>=2.0                     # assistant vector index (no vector DB needed)
