"""Pydantic schemas — the contract between React, FastAPI and the agent.

Everything crossing an HTTP or MCP boundary is defined here so the shape
of the data is declared in exactly one place.

TODO(step 2): LabInput, ReferenceRange, ClassifiedResult, AnalyzeResponse.
"""
